The HasPayrollCalculations trait has been updated to dynamically calculate taxable and non-taxable earnings and deductions from the form data, resolving an issue where these values were previously hardcoded. Additionally, the `reactive()` call has been added to the `tax_status` fields in the `ad_hoc_earnings` and `ad_hoc_deductions` repeaters in `EditPayroll.php` to ensure that any changes to the tax status of an earning or deduction will trigger a recalculation of the payroll. The calculation methods in `HasPayrollCalculations.php` have been updated to merge, unique, and then process the earnings and deductions to prevent double-counting. A hidden `id` field has been added to the `ad_hoc_deductions` repeater. A mislabeled placeholder for taxable deductions has been corrected. The `fetchApplicableComponents` method in `EditPayroll.php` has been updated to include the `tax_status` of salary components, and the calculation methods in `HasPayrollCalculations.php` have been updated to include these components in the totals. The attendance-related earnings and deductions are now included in the payroll calculation with the correct tax status as per the user's clarification. The `reactive()` call has been added to the attendance-related toggles in `EditPayroll.php` to ensure the payroll recalculates when they are toggled. The `reactive()` call has been added to the `tax_status` field in the `fund_reimbursements` repeater. The `getGrossSalary` method in `HasPayrollCalculations.php` has been updated to include the increment logic. The `InteractsWithFormActions` trait and required methods have been added to `EditPayroll.php` to fix a crash. The fund deduction logic has been simplified to use the `fund_data` from the form state, resolving a calculation discrepancy.